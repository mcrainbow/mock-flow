export enum GuestRoutesTypes {
  HOME = '/',
  ABOUT = '/about',
  LOGIN = '/login',
  SIGNUP = '/signup',
  NOT_FOUND = '*',
}

export enum UserRoutesTypes {
  INTERVIEW = 'interview',
  DASHBOARD = 'dashboard',
  PROFILE = 'profile',
  SETTINGS = 'settings',
}
